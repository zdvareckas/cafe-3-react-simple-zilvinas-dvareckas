import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#74665F',
    },
    secondary: {
      main: '#64493B',
    },
  },
  background: {
    default: '#fafafa',
  },
});

export default theme;
